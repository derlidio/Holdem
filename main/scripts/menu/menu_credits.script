function on_message(self, message_id, message, sender)

	if message_id == hash("click") then
		msg.post("/menu/menu", "switch")
		factory.create("/table#credits", vmath.vector3(640,360, 0.9), nil, nil, vmath.vector3(1.25, 1.25, 1.25))
	end
	
end